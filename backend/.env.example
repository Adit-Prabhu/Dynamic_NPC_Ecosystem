# Model Provider: "gemini", "openrouter", or "template"
NPC_MODEL_PROVIDER=gemini

# === DIALOGUE SETTINGS ===
# Delay between dialogue exchanges in seconds (for autoplay)
# Free tier APIs: use 5+ to avoid rate limits
NPC_DIALOGUE_DELAY=5

# === GEMINI CONFIG (recommended) ===
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-2.0-flash-lite
GEMINI_TEMPERATURE=0.7

# === OPENROUTER CONFIG (alternative) ===
# Get your free API key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=your-openrouter-api-key
# Free models on OpenRouter:
#   google/gemini-2.0-flash-exp:free    - Gemini 2.0 Flash (free tier)
#   meta-llama/llama-3.2-3b-instruct:free - Llama 3.2 3B
#   qwen/qwen-2-7b-instruct:free        - Qwen 2 7B
#   mistralai/mistral-7b-instruct:free  - Mistral 7B
#   google/gemma-2-9b-it:free           - Gemma 2 9B
OPENROUTER_MODEL=google/gemini-2.0-flash-exp:free
OPENROUTER_TEMPERATURE=0.7

# === RUMOR SEEDS ===
# Pipe-delimited list of starting rumors (one is picked randomly at boot)
NPC_RUMOR_SEEDS="Vault door left ajar|Smuggler spotted slipping into the sewers|Temple bells rang at midnight with no priest|Mayor's seal found on forged tax slips|Wyvern egg missing from the caravan"

# === OPTIONAL: NPC ROSTER CONTROL ===
# Restrict which NPCs can spawn (pipe-delimited keys)
# NPC_PERSONA_POOL="shopkeeper|guard|smuggler|bard|artificer|herbalist"
# How many NPCs in the conversation
# NPC_PARTY_SIZE=2
